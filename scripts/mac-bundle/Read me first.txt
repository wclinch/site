Site v0.1.0 — macOS

Install
  Double-click  Install Site.command
  Click "Allow" if macOS asks.
  Site installs and launches automatically.

Activation
  A license key was issued at purchase. Paste it into the gate on
  first launch of the workspace.

Support
  Official_Site_Support@protonmail.com

────────────────────────────────────────────

Manual install (only if the installer doesn't run)
  1. Drag Site.app into the Applications folder.
  2. Open Terminal and paste:
       xattr -cr /Applications/Site.app
  3. Launch Site.

Why the xattr step?
  Site is not notarized by Apple (notarization requires the $99/yr
  Apple Developer Program). macOS therefore blocks it on first
  launch. The command removes the "downloaded from the internet"
  flag. The application itself is unchanged.
